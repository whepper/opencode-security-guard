{
  // Optional: merge per-server trust statements into the compiled policy.
  // Values: "trusted" | "restricted" | "untrusted" | "blocked"
  "mcpServers": {
    // "outline": {
    //   "trust": "trusted",
    //   "reason": "Personal wiki — own documents only"
    // },
    // "github": {
    //   "trust": "restricted",
    //   "reason": "Read issues/PRs; writes need a prompt"
    // }
  },

  // Optional: override the effect for specific MCP tools.
  // Key format: "<server>_<tool>"
  // Effect: "allow" | "ask" | "deny"
  "mcpToolOverrides": {
    // "outline_create_document": { "effect": "allow" },
    // "outline_update_document": { "effect": "allow" },
    // "outline_delete_document": { "effect": "allow" },
    // "outline_list_documents": { "effect": "allow" }
  },

  // Optional: promote every ask-tier path rule to deny.
  // "promoteAskToDeny": false,

  // Optional: experimental provenance tracking.
  // "provenance": {
  //   "enabled": false
  // }

  // Optional: allowlist path prefixes whose SCRIPT WRITES skip the
  // content ask (GGW-CONTENT-001/002) — for trusted locations whose
  // scripts legitimately mention protected names (a security guard's own
  // test corpus, generators). Entries are directory prefixes (trailing
  // slash optional) or exact file paths. Operator-only: this file is
  // agent-write-denied (GG-SLF-001), so allowlisting can never be done by
  // the agent itself. Default: absent (content asks enforce as blocks on
  // betas where the permission channel cannot carry written content).
  // "contentWriteAllowlist": [
  //   "tests/",
  //   ".redteam/"
  // ]
}